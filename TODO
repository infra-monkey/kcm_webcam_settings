code:
use "v4l2-ctl -d /dev/video0 --info" to get the proper name and bus of the card
add missing controls
manage capture format and resolution
add save/restore/reset-to-default features
apply on plasma session start

UI:
allow to preview the camera in the page
